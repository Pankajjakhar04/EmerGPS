#!/bin/sh

current_branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$current_branch" = "main" ]; then
  echo "Direct push to main is blocked. Push a feature branch and open a PR."
  exit 1
fi

exit 0
